<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>{% block title %}Вход{% endblock %}</title>
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
    />
    {% block stylesheets %}{% endblock %}
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light" style="height: 70px;">

      <div class="collapse navbar-collapse" id="navbarSupportedContent"></div>
      <ul class="nav navbar-nav navbar-right">
        {% if app.user %}
            <li><a class="nav-link" href="{{ path('list') }}">Список криптовалют</a></li>
            <li><a class="nav-link" href="{{ path('app_logout') }}">Выход</a></li>
        {% else %}
            <li><a class="nav-link" href="{{ path('app_login') }}">Вход</a></li>
            <li><a class="nav-link" href="{{ path('registration') }}">Регистрация</a></li>
        {% endif %}
      </ul>
    </nav>
    {% block body %}{% endblock %} {% block javascripts %}{% endblock %}
  </body>
</html>